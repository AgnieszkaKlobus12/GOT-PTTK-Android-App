<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.poapp">

    <application
        android:allowBackup="true"
        android:fullBackupOnly="true"
        android:icon="@drawable/e_got_pttk"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".view.przodownik.PotwierdzActivity"
            android:exported="true"
            android:theme="@style/Theme.przodownik_theme" />
        <activity
            android:name=".view.turysta.trasy.ZapisanieTrasyActivity"
            android:exported="true"
            android:theme="@style/Theme.turysta_theme" />
        <activity
            android:name=".view.employee.mountainPass.MountainPassesActivity"
            android:exported="true"
            android:theme="@style/Theme.pracownik_theme" />
        <activity
            android:name=".view.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.main_theme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>